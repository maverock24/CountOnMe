// app/utils/intensity.enum.ts

export enum IntensityLevel {
  Light = 'light',
  Moderate = 'moderate',
  Hard = 'hard',
}

// Add FitnessLevel enum
export enum FitnessLevel {
  Beginner = 'beginner',
  Intermediate = 'intermediate',
  Expert = 'expert',
}
